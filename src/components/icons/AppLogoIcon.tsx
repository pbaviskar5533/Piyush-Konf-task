
import { Clapperboard } from 'lucide-react';
import type { LucideProps } from 'lucide-react';

const AppLogoIcon = (props: LucideProps) => (
  <Clapperboard {...props} />
);

export default AppLogoIcon;
